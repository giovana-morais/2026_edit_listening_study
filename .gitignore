# original audio
*.zip

# original audio name and mappings
assets/manifest.csv
assets/README.txt
assets/*/sdedit_rms.wav
assets/*/v4.wav

# real system identity behind the anonymized v1..v4 tokens — never publish
system-mapping.secret.json

# raw participant exports and decoded aggregate results — private until published
scripts/*.csv
listening-test-v4/
